import { style } from 'typestyle'
import { px } from 'csx'

export let wrapper = style({
    textAlign: 'center',
    padding: px(25)
})

export let image = style({
    height: px(60),
    width: px(60)
})
